using UnityEngine;
using System.Collections;

/// <summary>
/// Скрипт является вспомогательным и служит для того, чтобы передавать в мобов, которых мы спавним, параметры в виде их пути
/// </summary>
public class TriggerHelper : MonoBehaviour {

    /// <summary>
    /// Препятствия
    /// </summary>
    public GameObject[] Triggers;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
